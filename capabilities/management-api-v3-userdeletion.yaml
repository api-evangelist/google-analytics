naftiko: 1.0.0-alpha2
info:
  label: Google Analytics API — userDeletion
  description: 'Google Analytics API — userDeletion. 1 operations. Lead operation: userDeletion. Self-contained Naftiko capability
    covering one Google Analytics business surface.'
  tags:
  - Google Analytics
  - userDeletion
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GOOGLE_ANALYTICS_API_KEY: GOOGLE_ANALYTICS_API_KEY
capability:
  consumes:
  - type: http
    namespace: management-api-v3-userdeletion
    baseUri: https://analytics.googleapis.com/analytics/v3
    description: Google Analytics API — userDeletion business capability. Self-contained, no shared references.
    resources:
    - name: userDeletion-userDeletionRequests:upsert
      path: /userDeletion/userDeletionRequests:upsert
      operations:
      - name: analyticsuserdeletionuserdeletionrequestupsert
        method: POST
        description: Insert or update a user deletion requests.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: bearer
      token: '{{env.GOOGLE_ANALYTICS_API_KEY}}'
  exposes:
  - type: rest
    namespace: management-api-v3-userdeletion-rest
    port: 8080
    description: REST adapter for Google Analytics API — userDeletion. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/userdeletion/userdeletionrequests-upsert
      name: userdeletion-userdeletionrequests-upsert
      description: REST surface for userDeletion-userDeletionRequests:upsert.
      operations:
      - method: POST
        name: analyticsuserdeletionuserdeletionrequestupsert
        description: Insert or update a user deletion requests.
        call: management-api-v3-userdeletion.analyticsuserdeletionuserdeletionrequestupsert
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: management-api-v3-userdeletion-mcp
    port: 9090
    transport: http
    description: MCP adapter for Google Analytics API — userDeletion. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: insert-update-user-deletion-requests
      description: Insert or update a user deletion requests.
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: management-api-v3-userdeletion.analyticsuserdeletionuserdeletionrequestupsert
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
